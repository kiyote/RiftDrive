@page "/auth/validate"
@inherits ValidatePageBase

    <div class="container">
        <div class="row">
            <div class="col-sm">
                <h4>Signing In</h4>
            </div>
        </div>
        <div class="row">
            <div class="col-sm">
                <h5>Please hang on...</h5>
            </div>
        </div>
        <div class="row">
            <div class="col-sm">
                <div class="progress">
                    <div class="progress-bar" role="progressbar" style="width: @(Progress)%"></div>
                </div>
            </div>
        </div>
        @foreach (var message in Messages)
        {
            <div class="row">
                <div class="col-sm">
                    <span>@message</span>
                </div>
            </div>
        }
    </div>

<!--
<Row>
    <Column ColumnSize="ColumnSize.Is4.OnDesktop.Is3.OnMobile">
        <Card Margin="Margin.Is4.FromBottom.Is5.FromLeft.Is5.FromTop">
            <CardHeader>
                <CardTitle>
                    Signing In
                </CardTitle>
                <CardSubtitle>
                    Please hang on...
                </CardSubtitle>
            </CardHeader>
            <CardBody>
                <ProgressGroup>
                    <ProgressBar IsStriped="true" IsAnimated="true" Value=@Progress />
                </ProgressGroup>
            </CardBody>
            <CardBody>
                <CardText>
                    @foreach (var message in Messages)
                    {
                        <p>@message</p>
                    }
                </CardText>
            </CardBody>
        </Card>
    </Column>
</Row>
-->
